 <%- include  ('partials/header') %>

<h1>INdex connected</h1>

<% blogs.forEach(function(blog){ %>
	<div>
	      <h1><%= blog.title %></h1>
	      <img src ="<%= blog.image %>" >
	      <div><%= blog.body %> </div>
	      <p><%= blog.Created %></p>
	</div>
<% }) %>

<%- include   ('partials/footer') %>

